<div class="container">
  <div>
    <h1><%= @offer.title %></h1>
  </div>
  <div>
    <h3><%= @offer.category %></h3>
    <p><%= @offer.description %></p>
  </div>
  <div>
    <p><%= "Pick up at #{@offer.pick_up_at}" %></p>
    <h3><%= "#{@offer.price} €/ day" %></h3>
  </div>


    <%= link_to 'Book now', new_offer_booking_path(@offer), class: 'btn btn-primary'%>
</div>